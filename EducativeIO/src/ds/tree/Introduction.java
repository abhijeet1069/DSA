package ds.tree;

/**
 Tree - Hierarchical Data Structure
 Depth and Height are reverse of each other. Root node have max height, however leaf nodes have max depth.

 The most common trees are:
    1) N-ary Tree - Each node can have child nodes anywhere from 0 to N.
    2) Balanced Tree - All leaf nodes are at the same level (height balanced)
    3) Binary Tree - Max of 2 child nodes allowed.
    4) BinarySearch Tree - NodeValues(LeftSubtree)<=CurrentNodeValue<NodeValues(RightSubTree)
    5) AVL Tree - Special type of binary search tree, where for each Node, the maximum height difference between the left and right subtrees can only be one.
    6) Red-Black Tree - Special type of AVL tree with color
    7) 2-3 Tree - Type of search tree. Used in storage.
 * */

public class Introduction {
}
